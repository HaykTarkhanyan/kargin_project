# PROGRESS

Tracks what's done, in progress, and next. Update as work happens.

## Status
Pre-rewrite. The existing codebase is the original 2-hour vibecoded version (Streamlit + Telegram bot over a static CSV with fuzzywuzzy search). A significant recreation is planned but not yet started.

## Done
- 2026-04-25: Audited existing project, created `CLAUDE.md`, `PROGRESS.md`, `LEARNINGS.md`, `NOTES.md` scaffolding for the recreation.
- 2026-04-25: Moved the original codebase into `old/` via `git mv` (history preserved as renames). Root now holds only the four memory files plus `kargin_eng.csv`.
- 2026-04-25: Wrote `PLAN.md` with phased roadmap (open decisions → data pipeline → search → UI → bot → polish). Added "song extraction" to future-ideas section.
- 2026-04-25: Built `scripts/fetch_youtube_metadata.py` — yt-dlp-backed metadata fetcher, idempotent, schema in `data/youtube_metadata.csv`. Smoke-tested on 3/695 videos. See `LEARNINGS.md` for findings (YouTube auto-captions transcribe Armenian as Russian/Spanish, so they're unusable).
- 2026-04-25: Set up `pyproject.toml` and `uv` venv (Python 3.11.13). Made the yt-dlp script parallel (ThreadPoolExecutor, 8 workers). Hit YouTube anti-bot rate-limiting at ~150 requests; cookies workaround blocked by Chromium DB lock.
- 2026-04-25: Switched to YouTube Data API v3 (`scripts/fetch_youtube_metadata_api.py`). Whole archive metadata fetched in ~1 sec across 14 batched calls. Result: **695 rows, 0 failures, 33.55 hours total audio, all but 1 video public, zero usable captions.** Cost estimate for Gemini STT: $3-30 depending on model — Phase 0's "is this affordable" question is settled.
- 2026-04-25: Cross-referenced `internal/` curation files (3 spreadsheets, 600 unique video_ids). Found 6 videos missing from `kargin_eng.csv` — all alive on YouTube. Added the 6 to `kargin_eng.csv` (2 with hand-curated phrases from internal, 4 with title+link only). Refetched their API metadata. **All three data sources now consistent: kargin_eng.csv has 702 rows / 701 unique IDs, data/youtube_metadata.csv has 701 rows / 701 unique IDs, internal/ ⊂ both.** EDA notes: archive is 1 channel (KarginTV), 1 language (`hy` for 700/701), 33.6 hours total, 488M cumulative views, sketch numbering 75-702 with ~40 gaps.
- 2026-04-25: Initial commit of the rewrite scaffolding at `9e8708b` ("Reset for rewrite: archive old code, add planning + data pipeline scaffolding"). 31 files changed, 18 renames into `old/`. Working tree clean. Branch is 1 commit ahead of `origin/main`, not pushed.
- 2026-04-25: Karpathy's four behavioral rules (Think Before Coding / Simplicity First / Surgical Changes / Goal-Driven Execution) added to global `~/.claude/CLAUDE.md`. Project CLAUDE.md keeps only the project-specific addenda (rewrite-not-refactor, plan-first, scope discipline, subagent for exploration, `@file` references).
- 2026-04-26: Filled the missing YouTube link for sketch 672 (`hp0U2719O0A`, 176s, public, `hy`, uploaded 2013-04-11) — the one row that was curated but unlinked. Refetched its metadata via the API (resume logic skipped the other 701). **Both CSVs now: 702 rows, 702 unique video_ids, zero duplicates, zero diff between the two id sets.**
- 2026-04-26: Wrote `scripts/download_audio.py` — single-threaded, no-cookies, resume-by-file-presence audio downloader using `yt-dlp` `bestaudio/best`. Detects YouTube's bot-detection error and stops early. `data/audio/` and `data/video/` added to `.gitignore`. Smoke test on 1 video returned a 2.8 MB webm/opus file at ~111 kbps. Ready for a full run; expect to hit the rate-limit wall around ~150 videos and need multiple sessions to finish all 702.
- 2026-04-26: Project `CLAUDE.md` updated with a Context7 reminder (use the MCP to fetch live docs when touching external libraries, instead of trusting training-data syntax). Stale "701 rows" reference in CLAUDE.md repo layout corrected to 702.
- 2026-04-26: Renamed audio downloads to `{seq:03d}_{title}_{video_id}.{ext}` format (sequential seq from metadata.csv row order — sketch numbers are not unique enough to use as id: 4 dupes among 598, 104 titles lack the pattern). Resume regex anchors to the 11-char id before the extension, so it works across naming schemes. Single-video 360p experiment landed 8.32 MB in 7.3 s for a 201-sec sketch (format 18, pre-merged mp4). Extrapolation: full archive at 360p ≈ 4.7 GB; audio-only ≈ 1.6 GB.
- 2026-04-26: Wrote `scripts/download_video.py` as a parallel of the audio downloader (same naming, same resume, same rate-limit auto-stop) targeting `best[height<=360]/best`. Output goes to `data/video/` (gitignored). Not run in bulk yet.
- 2026-04-26: Full-archive audio download completed in **one run, no rate-limit wall**: 702/702 videos, 0 failures, 1.6 GB total, 44:54 wall time, ~3.84 sec/video. Cookieless, single-threaded. Files in `data/audio/{seq:03d}_{title}_{video_id}.{ext}` format, mostly webm/opus at 100-135 kbps. Updated LEARNINGS.md — the previously documented "~150-request wall" was specifically a parallel-fetch artifact; serial yt-dlp does not trigger it at archive scale.
- 2026-04-26: Full-archive transcript fetch via `scripts/fetch_transcripts.py` (yt-dlp, `subtitleslangs=['.*-orig']` regex to grab source-language auto-captions only, no auto-translation noise). 315 caption files + 388 sentinels (`.no_captions`), 0 failures, ~30 min wall time. Coverage: **314 of 702 videos (44.7%) had any captions on YouTube.** Source-lang distribution is *much* wider than the small samples suggested: tr=92, hy=84, en=49, ru=24, ro=20, hi=15, plus ar/de/fr/it/es/nl/pl/pt/bg/lv/el (each 1-4). Only **84 videos (12%) have genuine Armenian source captions**; the rest are STT mishearing the song intros. Total: 9062 events, 176,764 chars across all sources.
- 2026-04-26: Wrote `scripts/convert_transcripts.py` to flatten YouTube's verbose JSON3 (with `pens`, `wsWinStyles`, `wpWinPositions`, ms-based timing, nested `events[].segs[].utf8`) into a simplified per-video JSON: `{video_id, seq, title, source_lang, n_events, duration_sec, full_text, events: [{start, end, text}, ...]}`. Renamed raw output to `data/transcripts_raw/`; conversion produced fresh `data/transcripts/`. Both directories gitignored.
- 2026-04-26: Branch hygiene reset. The 3 unpushed commits (`9e8708b`, `1bc8094`, `8d08a94`) were moved off `main` onto a new `dev` branch and pushed to `origin/dev`. `main` is back at `origin/main` (the README commit `779665b`). Future experimental work should land on `dev` until ready to merge.
- 2026-04-26: **Gemini STT spike done. Model = `gemini-3.1-flash-lite-preview`** (the Lite variant — Flash 3.1 split into Lite/Live/TTS, Lite is the batch STT one and is free on the free tier). Ran on 6 hand-picked videos (3 hy-orig as ground truth, 3 wrong-source-lang as song-intro confounders), 17.3 min of audio, 97s wall, $0.0284 paid-tier cost (free-tier $0). All 6 returned valid JSON with `full_text` + `segments` (clause-level timestamps). Quality results:
  - **hy_orig**: Gemini text is more readable than YouTube's hy-orig — proper punctuation, full sentences, music omitted (vs YouTube interleaving `[երաժշտություն]`). Both make recognition errors but Gemini's stay grammatical.
  - **confounders**: Clean win. All 3 videos that YouTube mangled into Romanian/Turkish/Spanish garbage produced correct Armenian dialogue under the prompt "Transcribe SPEECH ONLY in Armenian. Ignore song lyrics... The video may open with a Russian or Spanish song before the dialogue starts; skip it." Output volume 4-15x larger than YouTube's wrong-language captions.
  - **Cost extrapolation to full 33.6h archive**: ~$3.32 paid tier, ~190 min wall serial. Free tier likely covers it.
  - Outputs: `scripts/gemini_stt_spike.py`, `scripts/build_spike_comparison.py`, `data/transcripts_gemini/{spike_summary.json, comparison.md, *.json}`. Webm→ogg-vorbis transcoding via ffmpeg is required (webm not in Google's documented supported formats).
- 2026-04-26: **Gemini 3.1 Pro spike done** for side-by-side quality comparison. Pro is paid-only (free-tier returned `limit: 0`); user enabled billing on the Google AI Studio project, then re-ran. Same 6 videos: 320s wall, $0.183 paid (vs Lite's 97s, $0.028 — 7x slower, 6.5x more expensive). Output token volume similar (10,800 Pro vs 10,034 Lite). Quality is **meaningfully better** on Armenian: Pro matched manual `kargin_eng.csv` text closely on the smoke video where Lite produced gibberish opening lines (Lite: `Պետրոս, սաղ օրն ելեք պապաներիդ...` vs Pro: `Պետրոսյան, ուրեմն էն երեկվա պայմանագրի թղթերն...` — manual: `Պետրոսյան ուրեմն երեկվա պայմանագրի թղթերն...`). Pro also got `Նասդակը` vs Lite's `Նաս դարկը` (world-knowledge disambiguation). Empirically confirmed Pro audio-in price ≈ $1.99/M (matches the $2/M estimate). Refactored `gemini_stt_spike.py` to take `--model lite|pro` via a `MODEL_REGISTRY` dict; outputs are now namespaced `{seq}_{video_id}__{model_short}.json` so multiple models coexist without overwriting. `build_spike_comparison.py` auto-discovers model summaries via `glob('spike_summary__*.json')` and now includes the manual `kargin_eng.csv` `text`/`text_common` as a third reference column alongside YouTube and Gemini outputs. Full-archive Pro extrapolation: ~$21.37 paid, ~9.7 hours wall serial.
- 2026-04-26: Checked the YouTube transcripts (`data/transcripts/`, `data/transcripts_raw/`) and Gemini spike outputs (`data/transcripts_gemini/` minus the regeneratable `_tmp_ogg/`) into git. ~8.4 MB total. `.gitignore` updated to keep only `_tmp_ogg/` ignored under `transcripts_gemini/`.
- 2026-06-16: **Built and deployed the public website MVP** (jumped ahead to PLAN.md Phase 3). Static **Next.js 16 + Tailwind v4** app in `web/`, "Kargin Classic" design (warm paper, hard ink borders + offset shadows, Armenian flag-color accents, Anton + Noto Sans Armenian). Surfaces: home/search (instant client-side **substring** search over curated text + facets, composable filters, sort, 48-cap + lazy thumbnails), **702 static `/sketch/[id]` watch pages** (embedded player + curated dialogue + disabled timestamp chips as the transcript-ready seam), random, about. Python build (`scripts/build_site_data.py` + `scripts/kargin_build/`) joins the two CSVs → `web/public/data/sketches.json` with facet canonicalization (real `titles`, ~602/702 have dialogue). Deployed to GitHub Pages via Actions, **live at https://hayktarkhanyan.github.io/kargin_project/** (basePath `/kargin_project`, public). Spec + plan in `docs/superpowers/`, both independently reviewed (caught: dead random-sort, fabricated titles, Tailwind-v4 config error, MiniSearch substring gap). Built via 5 batched subagent passes. **Search is curation-bounded** (only 602 sketches have dialogue text) until transcripts land.

- 2026-06-16: **Website v2 shipped.** Stats/EDA page (`/stats`), Find-my-name page (`/find-my-name`), filters moved into the **hero top-right** (full-width 4-col grid), watch-page right column **redesigned** (catchphrase + clickable actor/location tags + **rarity-weighted related sketches**; curated dialogue retired from public view), and **copy-link / copy-quote** buttons. New `scripts/kargin_build/stats.py` precomputes `web/public/data/stats.json` (co-occurrence, words/bigrams, composition, aggregates, name suggestions). Charts are **hand-rolled SVG/CSS** (scatter, heatmap, trend line, word field, bars) — no chart lib. Find-my-name uses **declension-aware word matching** (Անի → 22 real mentions, not 365 raw-substring false positives). Spec + plan in `docs/superpowers/`, self-reviewed in 3 passes (caught: substring over-match, related-by-actor degeneracy from the 76% Հայко/Мко duo, `useSearchParams`/Suspense build-breaker, a cut-scope cross-link contradiction). Built via 5 batched subagent passes.
- 2026-06-16: **Search + mobile polish shipped** (built via **two parallel git-worktree agents**). **Romanized/Latin search** — type `tormuz`/`hayko` and match `տոռմուզ`/`Հայко` (each sketch's text dual-indexed with a phonetic Armenian→Latin `romanize()` in `web/lib/translit.ts`); **typo-tolerant fuzzy** via word-level **Fuse.js** (threshold 0.35) merged after exact matches. **Mobile responsive pass** — hamburger nav on `<md` (the 5-item nav overflowed at 375px), scaled headings (`text-4xl sm:text-6xl`), tighter padding, heatmap wrapped in `overflow-x-auto`. 33 vitest tests, clean build, verified at 375px.

## In progress
- Nothing actively in progress. Website MVP + v2 are live; data-pipeline Phase 1 (transcription) is the main unbuilt piece.

## Next
- **Phase 1A — full archive transcription** is now the highest-leverage step: the site already reserves `segments: []` per sketch and the watch view has disabled timestamp chips, so filling segments lights up timestamped jump-to-moment AND lifts search recall from 602→702. Run `gemini_stt_spike.py` (or a generalized `transcribe_archive.py`) over all 702 videos; free tier should cover it, else ~$3-4. Idempotent already.
- **Wire transcripts into the site:** extend `build_site_data.py` to populate `segments[]` from the Gemini JSON; make the watch-view timestamp chips clickable `&t=Ns` deep-links.
- **Deferred website features**: usage logging (browser→Supabase, no backend) and semantic search (needs client-side embeddings on static hosting). *(Stats + Find-my-name shipped in v2.)*
- Phase 1B (metadata enrichment) can run in parallel.
- **Housekeeping:** bump the GitHub Actions in `deploy.yml` to Node 24-compatible versions (deprecation warning on the deploy run).
